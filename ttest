def is_inconsummate(n):
    '''
    A inconsummate number is a non-negative integer that cannot be formed by any integer N divided by the the digital sum of N
    '''
    digitalsum = sum(d * 10^i for i, d in enumerate(reverse([int(c) for c in str(n)])))
    max
    for i in range(0, 

def generate_inconsummate():
    count = 0
    for n in 0:10^9
        if is_inconsummate(n):
            count += 1
            yield n, count


