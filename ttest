    edges = [[2,4], [4,1], [4,1],[1,3],[1,3],[2,3]]
    G = DiMultigraph(4)
    for edg in edges
        add_edge!(G, edg[1], edg[2])
    end

    e = edges(G)
    e1 = sort!(collect(e), by = edg -> findfirst(==([edg.src, edg.dst]), edges))
    
