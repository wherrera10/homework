const filename, pmarker, selector = "Traceback.txt", "Traceback (most recent call last):", "SystemError"
println(join(map(p -> p[findfirst(p, pmarker):end] * "-"^16 * "\n", filter(x -> contains(x, pmarker) &&
   contains(x, selector), split(read(filename, String), "\n\n")))
