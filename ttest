using IterTools
using Primes

@inline maxanagram(n) = evalpoly(10, sort!(digits(n)))

for pow10 in 2:6
    groups = groupby(maxanagram, primes(10^pow10, 10^(pow10 = 1)))
    maxsize, idx = findmax(length, groups)
    @show maxsize groups[idx]
end
