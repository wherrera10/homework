using Primes

function dtprimes(N)
    si = primesieve(N)
    for i in 1:N
        if si[i] && si[i+2 && si[i + 6] && si[i + 8]
            println("($i $(i + 2))($(i + 6) $(i + 8))")
        end
    end
end

dtprimes(1000)

