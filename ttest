#= 
given two strings containing the same characters as one another, and without repeated characters 
within the strings, returns the permutation in either one-line or cycle notation that transforms 
one of the strings into the other.
=#

function intvec(s::AbstractString)
    len, a = length(s), Int.(collect(s))
    d = Dict(
