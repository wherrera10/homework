julia> issquare(n) = isqrt(n)^2 == n
issquare (generic function with 1 method)

julia> issquare(121)
true

julia> issquare(37)
false

julia> issquare(36)
true

julia> is_subunit_square(n) = issquare(n) && issquare(subunit(n))
is_subunit_square (generic function with 1 method)

julia> subunit(n, base=10) = evalpoly((digits(n, base=base) .- 1), base)

subunit (generic function with 2 methods)

julia> subunit(n, base=10) = evalpoly((digits(n, base=base) .- 1), base)
