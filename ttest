using Primes

wolstenholme(n) = sum(1 // i*i for i in big"1":BigInt(n)).num

function process_wolstenholmes()
    wols = [wolsteholme(n) for n in 1:1000]
    pwols = filter(isprime, @view wols[begin:begin+2000])

end


        
