const nouns = split(read("noun_list.txt", String), "\s+")
const adjectives = split(read("adjective_list.txt", String), "\s+")

if rand() < 0.5
    println("The ", rand(nouns, 1), " and ", rand(nouns, 1))
else
    println("The ", rand(adjectives, 1), " ", rand(nouns, 1))
end
